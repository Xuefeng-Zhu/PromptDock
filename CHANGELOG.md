# Changelog

Notable changes are summarized per release.

PromptDock uses pull requests into `develop` for unreleased work. Release notes
are generated from merged pull requests when a `vX.Y.Z` tag is cut from `main`,
using the categories in `.github/release.yml`.

## Unreleased

- See open and merged pull requests targeting `develop`.
